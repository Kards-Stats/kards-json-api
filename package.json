{
  "name": "kards-json-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.44"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs -p 5678",
    "build": "openapi bundle -o dist",
    "test": "openapi lint"
  }
}
