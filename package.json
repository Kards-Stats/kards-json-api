{
  "name": "kards-json-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.49"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs -p 5678",
    "build": "openapi bundle -o dist && npx redoc-cli bundle dist.yaml --output build/index.html",
    "test": "openapi lint"
  }
}
